#include <iostream>
using namespace std;

int main()
{
    int arr[20];
    int n;
    int mid=0,low=0,high;
    cout<<"Enter the number of elements :";
    cin>>n;
    cout<<"Enter the array elemnets : ";
    for (int i = 0; i < n; i++)
    {
        cin>>arr[i];
        /* code */
    }

    high=n-1;
    while (mid<=high)
    {
        /* code */
    }
    
    
}